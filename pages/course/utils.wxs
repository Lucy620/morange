var utils = {
  checkStatus: function(courseID, courseList, isFree){
    return courseList.indexOf(courseID) == -1 || isFree ? '预约' : '免单'
  },
  mixList: function (list1, list2) {
    return list1.concat(list2);
  }
}

module.exports = {
  checkStatus: utils.checkStatus,
  mixList: utils.mixList
}